## Git

```
alias gc="git add . && git commit -am "
alias gcm="git checkout master"
alias gpull="git pull origin master"
alias gpush="git push origin master"
```

## Apache
```
alias h="sudo subl /etc/hosts"
alias ap="sudo apachectl restart"
```